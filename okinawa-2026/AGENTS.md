# Okinawa 2026 trip instructions

- This folder is a normal subfolder of the `Desktop\Trips` monorepo; it must not contain `.git`.
- `index.html` is the authoritative working HTML for this trip.
- Do not use stale ChatGPT/Gemini copies to overwrite it.
- Preserve the existing travel template unless explicitly asked to change it.
- Use `PUBLISH.cmd` to publish only `okinawa-2026` after review.
- Fetch remote state before publishing and stop on remote/local conflict.
- Never force push.
- Verify the new GitHub Pages URL after publish.
